import { INpmRcConfig } from '../inteface'

export const testConfig: INpmRcConfig = {
  registry:
    'https://pkgs.dev.azure.com/microsoftLearnModule/_packaging/microsoftLearnModule/npm/registry',
  username: 'microsoftLearnModule',
  token: 'hello',
  alwaysAuth: false,
}
